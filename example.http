# 🔧 Environment Variables
@baseUrl = http://127.0.0.1:8000
@model_id = binwang/RSE-BERT-base-STS
@dataset_id = BIREX-CompetenceCenter/SISMA-3DPrinter-EnergeticConsumption

# 📦 DATASETS ROUTES

### List datasets (API)
GET {{baseUrl}}/api/datasets
Accept: application/json

### Search datasets (API)
POST {{baseUrl}}/api/search/datasets
Content-Type: application/x-www-form-urlencoded
Accept: application/json

description = I'm searching for a predictive maintenance or anomaly detection dataset in the field of additive manufacturing (3D printing).

### Download dataset data file (placeholder.txt)
GET {{baseUrl}}/datasets/{{dataset_id}}/data?file_type=data
Accept: text/plain

### Download dataset CSV sample
GET {{baseUrl}}/datasets/{{dataset_id}}/data?file_type=sample
Accept: text/csv

### Download dataset - invalid file type (should return 400)
GET {{baseUrl}}/datasets/{{dataset_id}}/data?file_type=invalid
Accept: application/json

# 🤖 MODELS ROUTES

### List models (API)
GET {{baseUrl}}/api/models
Accept: application/json

### Search models (API)
POST {{baseUrl}}/api/search/models
Content-Type: application/x-www-form-urlencoded
Accept: application/json

description = text classification

### Download model weights file (placeholder.txt)
GET {{baseUrl}}/models/{{model_id}}/weights?file_type=weights
Accept: text/plain

### Download model CSV sample
GET {{baseUrl}}/models/{{model_id}}/weights?file_type=sample
Accept: text/csv

### Download model - invalid file type (should return 400)
GET {{baseUrl}}/models/{{model_id}}/weights?file_type=invalid
Accept: application/json

### Download model - default weights (no file_type param)
GET {{baseUrl}}/models/{{model_id}}/weights
Accept: text/plain
